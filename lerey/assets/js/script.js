// Simple chat icon interaction (toggle)
document.addEventListener("DOMContentLoaded", function () {
    const chatBtn = document.querySelector(".bi-chat-dots");
    chatBtn.addEventListener("click", () => {
        alert("Chat feature coming soon!");
    });
});
